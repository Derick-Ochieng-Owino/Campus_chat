rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Danger: Allows anyone to read and write. Use with caution.
    match /{allPaths=**} {
      allow read, write: if true; 
    }
  }
}